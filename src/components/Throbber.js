import React from 'react'

const Throbber = ({ visible }) => {
    console.log(visible)

    return (
        <div className="throbber">
            트로버
        </div>
    )
}

export default Throbber